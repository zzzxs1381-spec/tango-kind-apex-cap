# Copy to .env on each VPS. Never commit the real file.
WG_IP=10.77.0.2
XF_NODE_NAME=core-primary
XF_RELEASE=main

# Primary DB container settings
POSTGRES_DB=xfreedom
POSTGRES_USER=xfreedom
POSTGRES_PASSWORD=CHANGE_ME_LONG_RANDOM

# Application database URL
# Primary example:
DATABASE_URL=postgresql://xfreedom:CHANGE_ME_LONG_RANDOM@postgres:5432/xfreedom
# Secondary example:
# DATABASE_URL=postgresql://xfreedom:CHANGE_ME_LONG_RANDOM@10.77.0.2:5432/xfreedom

# Add Better Auth / provider / model-router secrets here as server-side env vars.
