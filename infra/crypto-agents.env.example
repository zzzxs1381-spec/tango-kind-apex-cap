# Crypto Agent Mesh — secrets belong only on the VPS, never commit real values.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-sol
SPOT_SYMBOLS=btcusdt,ethusdt,solusdt,bnbusdt,xrpusdt
EXECUTION_MODE=paper
MAX_ORDER_QUOTE=100
MIN_A_CLASS_PROBABILITY=0.85

# Binance SPOT trading only. Use trade permission only; do not grant withdrawals.
BINANCE_API_KEY=
BINANCE_API_SECRET=
BINANCE_BASE_URL=https://api.binance.com

# Optional live intelligence feeds.
X_BEARER_TOKEN=
ARKHAM_WEBHOOK_SECRET=
